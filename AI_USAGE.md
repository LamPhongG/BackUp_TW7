# AI Usage Declaration Log

| Date | Tool Name | Module / File Affected | Purpose | Changes / Verification Done | Verified By |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 2026-09-24 | Claude / Gemini | Initial Setup | Khởi tạo cấu trúc dự án | Kiểm tra khớp đặc tả đề bài | Team Lead |
| 2026-09-24 | Claude Code (Claude Opus 5.5) | `frontend/src/contexts/LanguageContext.jsx`, `frontend/src/locales/en.js`, `frontend/src/locales/vi.js` | Loại hỗ trợ: review code + sinh code. Rà soát logic chuyển đổi ngôn ngữ và viết lại i18n: `t()` có tham số và fallback tiếng Anh, `tv()`, `tNode()`, `pick()`, kiểm tra giá trị `localStorage` | Sửa: app crash khi `app_lang` không hợp lệ, key `close` bị trùng, thiếu `<html lang>`. Test: `npm run build`; render 31 route × `vi`/`en`/giá trị sai → 0 lỗi; đối chiếu key en/vi khớp 100% | Phạm Tấn Tài |
| 2026-09-24 | Claude Code (Claude Opus 5.5) | `frontend/src/pages/**`, `frontend/src/layouts/**`, `frontend/src/components/*` | Loại hỗ trợ: sinh code + sửa lỗi. Chuyển ngữ toàn bộ trang Employee / Manager / Admin / Auth; thêm nút đổi ngôn ngữ cho Manager và trang đăng nhập | Sửa lỗi: nút "Tiếp" bị cắt chữ, textarea AI Studio không đổi ngôn ngữ, quiz ghi cứng "2 / 3 đúng" (nay tính điểm thật), checklist đếm sai do làm tròn lên, link `/manager/settings` dẫn tới 404. Test: build + render toàn bộ route, quét chữ tiếng Anh còn sót ở chế độ VI | Phạm Tấn Tài |
| 2026-09-24 | Claude Code (Claude Opus 5.5) | `frontend/src/data/company.js`, `frontend/src/data/mock.js`, `frontend/src/utils/helpers.js`, `frontend/src/components/ValidationTag.jsx` | Loại hỗ trợ: sinh dữ liệu mẫu + code. Chuyển dữ liệu sang hồ sơ FourAngryBirds (10 vị trí, danh mục DOC-01…20); thêm giai đoạn Week 2 (SRS Step 13), trạng thái tiến độ (Step 54), đủ trạng thái kiểm định (mục 1.2), mức độ khó (Step 25) | Sửa: ngày tháng chuyển sang ISO và hiển thị theo locale; bỏ trạng thái cũ "At Risk". Test: build + render; kiểm tra badge/màu từng trạng thái | Phạm Tấn Tài |
| 2026-09-24 | Claude Code (Claude Opus 5.5) | `frontend/src/services/documentStore.js`, `frontend/src/contexts/DocumentsContext.jsx`, `frontend/src/utils/documentValidation.js`, `frontend/src/pages/admin/Documents.jsx`, `frontend/src/pages/admin/KnowledgeBase.jsx`, `frontend/src/pages/employee/Documents.jsx`, `frontend/src/pages/admin/AIStudio.jsx`, `frontend/src/styles/index.css` | Loại hỗ trợ: sinh code. Kho tài liệu cho HR: tải nhiều file, lưu IndexedDB, kiểm tra theo SRS Step 5 (định dạng qua magic bytes, dung lượng, file rỗng, trùng SHA-256, mã/phiên bản/ngày), quản lý phiên bản theo Step 8 | Sửa: side effect nằm trong updater `setState` của luồng upload cũ; bỏ phần giả lập "AI đang phân tích" và số chunk giả. Test: 26 unit test cho `documentValidation.js` (20 tên file của hồ sơ, PDF/DOCX giả, trùng hash/phiên bản, vòng đời phiên bản) → 26/26 đạt; build + render | Phạm Tấn Tài |
| 2026-09-24 | Claude Code (Claude Opus 5.5) | `frontend/` (xoá `update_fonts.*`, `layouts/AppShell.jsx`, `services/api.js`, 15 trang cũ ở `src/pages/*.jsx`), import thừa ở 7 file | Loại hỗ trợ: rà soát code chết. Tìm file và export không được import ở đâu | Đã kiểm tra từng file không còn được tham chiếu trước khi xoá. Test: build, 26 unit test, render 31 route sau khi dọn → không lỗi; quét import không dùng → 0 | Phạm Tấn Tài |
| 2026-09-24 | Claude Code (Claude Opus 5.5) | `frontend/README.md`, `AI_USAGE.md`, `.gitignore` | Loại hỗ trợ: soạn tài liệu. Mô tả công nghệ, cấu trúc, tính năng, kiểm thử, giới hạn; khai báo sử dụng AI; bỏ qua `node_modules/`, `dist/` | Đã đối chiếu nội dung README với code và kết quả test thực tế; ghi rõ các phần còn là dữ liệu mẫu | Phạm Tấn Tài |
| 2026-09-24 | Claude Code (Claude Opus 5.5) | `frontend/src/hooks/useAuth.js`, `frontend/src/layouts/ReviewerLayout.jsx`, `frontend/src/pages/reviewer/*`, `frontend/src/App.jsx`, `frontend/src/pages/auth/Login.jsx`, `frontend/src/locales/*` | Loại hỗ trợ: sửa code. Đổi vai trò Manager thành Reviewer (route `/reviewer/*`, link cũ `/manager/*` tự chuyển hướng); giữ phiên đăng nhập trong `sessionStorage` để audit log ghi đúng người thao tác | Test: build; E2E Chrome headless: đăng nhập Reviewer, tải lại trang vẫn giữ phiên, `/manager/team` chuyển về `/reviewer/dashboard` | Phạm Tấn Tài |
| 2026-09-24 | Claude Code (Claude Opus 5.5) | `frontend/src/services/{apiClient,textExtraction,documentProcessing}.js`, `frontend/src/utils/{chunker,injectionScan}.js`, `frontend/src/services/documentStore.js`, `frontend/src/contexts/DocumentsContext.jsx`, `frontend/src/components/DocumentProcessing.jsx` | Loại hỗ trợ: sinh code. Xử lý tài liệu (WBS Phase 2): trích xuất PDF (pdf.js) và DOCX (mammoth), chia chunk theo contract `{doc_id, chunk_id, section_id, heading, page, content}`, thanh tiến trình 0–100%, xem chunk; quét prompt injection EN/VI (Rules mục 5); nối sẵn `POST /upload` khi có `VITE_API_URL` | Test: 37 unit test (chunker + 11 kịch bản tấn công + văn bản bình thường không bị gắn cờ); E2E với 5 file thật (PDF 3 trang, DOCX có heading, MD, TXT tấn công, PDF chỉ có ảnh) trên cả dev và bản build | Phạm Tấn Tài |
| 2026-09-24 | Claude Code (Claude Opus 5.5) | `frontend/src/data/roleMatrix.js`, `frontend/src/utils/roleMatrix.js`, `frontend/src/contexts/RoleMatrixContext.jsx`, `frontend/src/pages/admin/RoleMatrix.jsx` | Loại hỗ trợ: sinh code + dữ liệu mẫu. Màn hình Role Requirement Matrix 10 vị trí × 20 tài liệu, chủ đề bắt buộc kèm từ khoá, nhập/xuất CSV, tính Coverage Score | Ma trận mặc định cần đối chiếu với `role_matrix.csv` của nhóm QA. Test: 8 unit test (coverage, CSV round-trip, dòng lỗi); E2E: bấm ô, tải lại trang vẫn giữ | Phạm Tấn Tài |
| 2026-09-24 | Claude Code (Claude Opus 5.5) | `frontend/src/utils/verification.js`, `frontend/src/services/pipelineService.js`, `frontend/src/contexts/ReviewContext.jsx`, `frontend/src/components/Citation.jsx`, `frontend/src/components/review/*`, `frontend/src/pages/review/*`, `frontend/src/pages/admin/AIStudio.jsx`, `frontend/src/pages/employee/Quiz.jsx`, `frontend/src/data/mock.js` | Loại hỗ trợ: sinh code. Phase 3: phân loại Verified / Verified with Warning / Manual Review Required; Dual Comparison View tô đỏ/vàng; hiển thị `exact_quote` và đối chiếu câu trích với chunk; Approve / Reject / Override kèm lý do; ghi đè từng trường; audit log chỉ thêm, xuất CSV. Bỏ `setTimeout` giả lập ở AI Studio | Output hai pipeline vẫn là dữ liệu mẫu, giao diện gắn nhãn *Chế độ demo*. Test: 17 unit test cho classifier và quy tắc quyết định; E2E đi hết luồng sinh → Override bị chặn khi lý do ngắn → ghi đè trường → phân loại lại → Approve → audit log 6 bản ghi; không lộ khoá dịch ở cả vi/en | Phạm Tấn Tài |
| 2026-09-24 | Claude Code (Claude Opus 5.5) | `frontend/src/pages/admin/Documents.jsx`, `frontend/src/components/UI.jsx`, `frontend/src/styles/review.css` | Loại hỗ trợ: sửa lỗi. Modal tải lên kẹt ở *Đang kiểm tra…* khi chạy dev (StrictMode làm cờ `mounted` không được bật lại); textarea ở AI Studio nằm cạnh nhãn; `Card` bỏ qua prop `style` | Test: tái hiện lỗi bằng E2E trên dev server, sửa xong chạy lại thì đạt | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `frontend/src/utils/{pathGenerator,pathChecks,pathWorkflow,progress}.js`, `frontend/src/contexts/{PathsContext,EnrollmentContext}.jsx`, `frontend/src/services/pipelineService.js` | Loại hỗ trợ: sinh code. Đổi phạm vi sản phẩm theo yêu cầu nhóm: web tạo lộ trình học từ tài liệu (không phải quản lý nhân sự). Sinh bản nháp lộ trình từ chunk; kiểm định kiến thức / luồng / chức năng / an toàn; vòng đời nháp → chờ duyệt → cần sửa → phát hành → thu hồi; tiến độ học và khoá giai đoạn | Test: 35 unit test mới (sinh lộ trình, kiểm định, phân quyền, tiến độ) — tổng 80/80 đạt. Bản nháp chưa phải output Gemini, giao diện gắn nhãn rõ | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `frontend/src/App.jsx`, `frontend/src/layouts/RoleLayout.jsx`, `frontend/src/hooks/useAuth.js`, `frontend/src/components/path/*`, `frontend/src/pages/{hr,reviewer,shared,employee}/*`, `frontend/src/styles/paths.css`, `frontend/src/locales/*` | Loại hỗ trợ: sinh code + dọn code. 3 vai trò HR / Reviewer / Nhân viên, chặn truy cập theo vai trò; Reviewer sửa trực tiếp, góp ý theo mục, trả về HR, phát hành theo phòng ban/vị trí; nhân viên học bài, mở tài liệu gốc đúng trang, làm nhiệm vụ, làm bài kiểm tra. Xoá các trang quản lý nhân sự và dữ liệu mẫu cố định (`data/mock.js`) | Test: build; E2E Chrome headless toàn luồng (tải 5 file → sinh → sửa → gửi duyệt → trả về → sửa → duyệt lại → phát hành → nhân viên học, trượt rồi đạt bài kiểm tra, giai đoạn sau mở); quét khoá dịch vi/en trên mọi trang → 0 lỗi; khoá en/vi khớp 594/594 | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `documentation/FRONTEND_FLOWS.md`, `frontend/README.md` | Loại hỗ trợ: soạn tài liệu. Mô tả chức năng, luồng thực hiện theo vai trò, vòng đời lộ trình, quy tắc kiểm định, cấu trúc dữ liệu, hợp đồng API và danh sách thay đổi | Đã đối chiếu với code và kết quả test thực tế | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `frontend/src/**`, `frontend/package.json`, `documentation/FRONTEND_FLOWS.md`, `frontend/README.md` | Loại hỗ trợ: dọn code. Quét file/export/CSS/khoá dịch không dùng; xoá trang đăng ký, quên mật khẩu và form đăng nhập giả; gỡ `recharts`; bỏ 24 icon, 223 selector CSS, 31 khoá dịch thừa; gộp CSS tính năng vào `features.css`; bỏ comment trang trí và comment mô tả "làm gì" | Test: build; 80/80 unit test; E2E toàn luồng chạy lại đạt; quét lại không còn file/export/import thừa | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `frontend/src/services/sanitize.js`, `frontend/src/components/ErrorBoundary.jsx`, `frontend/src/contexts/*`, `frontend/src/main.jsx`, `frontend/src/layouts/RoleLayout.jsx`, `frontend/src/App.jsx`, `frontend/index.html` | Loại hỗ trợ: sửa lỗi. Trắng trang khi trình duyệt còn dữ liệu cũ/hỏng: lọc dữ liệu khi đọc, thêm error boundary (trang báo lỗi + xoá dữ liệu demo), nút quay về ở trang 404, thêm favicon | Tái hiện lỗi bằng Chrome headless (5 loại dữ liệu hỏng × 3 vai trò), sửa xong không còn trang trắng; 6 unit test mới (86/86 đạt); E2E toàn luồng đạt | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `frontend/src/utils/pathChecks.js`, `frontend/src/utils/pathGenerator.js`, `frontend/src/services/pipelineService.js`, `frontend/src/pages/hr/CreatePath.jsx`, `frontend/src/components/path/*`, `frontend/src/App.jsx`, `frontend/src/layouts/RoleLayout.jsx`, `frontend/tests/*`, tài liệu | Loại hỗ trợ: sửa code theo yêu cầu nhóm. Bỏ Role Requirement Matrix khỏi frontend (thuộc Pipeline 2 backend Python): xoá trang, context, dữ liệu mặc định, hàm tính Coverage/CSV; kiểm định "Chức năng" chỉ hiển thị `coverage` backend trả về, chưa có thì cảnh báo | Test: build; 77/77 unit test; E2E toàn luồng đạt; quét lại không còn file/export/khoá dịch thừa | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `frontend/src/pages/auth/Login.jsx`, `frontend/src/layouts/AuthLayout.jsx`, `frontend/src/hooks/useAuth.js`, `frontend/src/contexts/LanguageContext.jsx`, `frontend/src/styles/*.css`, `frontend/src/locales/*` | Loại hỗ trợ: sinh code giao diện theo mẫu video *Modern Glassmorphism Login* và ảnh nhóm gửi. Giữ bố cục 2 nửa (giới thiệu + thẻ kính); form email/mật khẩu kiểm tra với 3 tài khoản demo, hiện/ẩn mật khẩu, ghi nhớ đăng nhập (localStorage/sessionStorage), hướng dẫn quên mật khẩu, chip điền sẵn tài khoản | Test Chrome headless: bỏ trống / sai mật khẩu / hiện mật khẩu / ghi nhớ / đăng xuất; ảnh chụp 1440px, 390px, tiếng Anh; 77/77 unit test; E2E toàn luồng đạt | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `frontend/src/hooks/useMyPaths.js`, `frontend/src/utils/helpers.js`, `frontend/README.md`, `documentation/FRONTEND_FLOWS.md` | Loại hỗ trợ: rà soát trước khi push. Quét file/export/import/CSS/khoá dịch/thư viện không dùng, `console.log`/TODO/chuỗi giống API key; chuyển `useMyPaths` về `hooks/`; thống nhất comment tiếng Việt | Không còn code thừa (các class CSS còn lại là class ghép động); build; 77/77 unit test; E2E toàn luồng đạt | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `backend/**` (app, alembic, tests, README, requirements), `.gitignore`, `frontend/.env.example`, `frontend/src/services/apiClient.js` | Loại hỗ trợ: sinh code + thiết kế. Dựng khung backend FastAPI theo Rules mục 2: config `.env`, SQLAlchemy 2 + Alembic (SQLite dev / PostgreSQL deploy), 13 bảng cho tài liệu, chunk, cờ injection, lộ trình (nội dung JSON), nguồn, phát hành, góp ý, audit log, tiến độ học; seed 10 phòng ban, 10 vị trí, 3 tài khoản demo (bcrypt); đăng nhập JWT, `/auth/me`, phân quyền theo vai trò, danh mục; API dưới `/api`; hook Alembic bỏ CHECK trùng của enum | Test: 26/26 pytest (đăng nhập 3 vai trò, sai mật khẩu, token sai/hết hạn, tài khoản bị khoá, phân quyền, CORS, ràng buộc DB, seed chạy lại, migration khớp model); `alembic check` không lệch; sinh lại migration từ DB trống khớp bản commit; cài `requirements.txt` trên venv sạch, `pip check` đạt; chạy uvicorn thật và gọi login/danh mục/Swagger | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `backend/app/{ingestion,services,schemas,api/routes}/*`, `backend/app/core/{errors,injection_filter}.py`, `backend/tests/*`, `backend/README.md` | Loại hỗ trợ: sinh code. Backend phần HR: kho tài liệu (tải lên kèm kiểm tra định dạng/magic bytes/dung lượng/trùng SHA-256/mã/phiên bản; trích xuất PDF, DOCX, TXT, MD, CSV; chunker và bộ lọc prompt injection chuyển từ frontend; vòng đời phiên bản; xem chunk; tải file gốc; thử lại; xoá có chặn khi đang dùng); lộ trình (tạo, sửa, sinh lại, gửi duyệt, gửi lại, xoá nháp, thu hồi, góp ý, đánh dấu đã xử lý) với bảng quyền kiểm tra ở server; audit log ghi cùng transaction; lọc dữ liệu theo vai trò; lỗi trả `code` trùng khoá dịch frontend | Test: 85/85 pytest (thêm 59 test: trích xuất file thật, PDF scan, mã hoá sai, luật injection EN/VI, API tài liệu, vòng đời lộ trình, phân quyền 403/404/409, audit); so khớp chunker Python với chunker JS trên 8 bộ dữ liệu → giống hệt; chạy server thật với 5 tài liệu mẫu của nhóm (PDF/DOCX/MD) → xử lý 16–150 ms, tạo lộ trình và gửi duyệt ghi đúng audit | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `backend/app/genai_pipeline/*` (client, schemas, prompts v1.0, grounding, local_draft, generator), `backend/app/services/{paths,path_checks}.py`, `backend/app/db/base.py`, migration `path generation report`, `frontend/src/{services/apiClient,services/apiMappers,hooks/useAuth,contexts/DocumentsContext,contexts/PathsContext}.js(x)`, `frontend/src/components/path/{GenerationReport,PathContent,PathComments}.jsx`, `frontend/src/pages/*`, `frontend/src/locales/*` | Loại hỗ trợ: sinh code + thiết kế. Pipeline 1: Gemini (google-genai, structured output) sinh bài học + nhiệm vụ rồi câu hỏi cho từng học phần song song; grounding loại/sửa mục không trích được nguyên văn tài liệu, câu hỏi có đáp án ngoài câu trích; bản nháp theo luật (chuyển từ pathGenerator.js) khi không có key hoặc Gemini lỗi; báo cáo sinh nội dung. Kiểm định phía server (chuyển từ pathChecks.js) khi gửi duyệt/duyệt; API Reviewer approve / request-changes. Frontend chạy được với backend: đăng nhập JWT, tài liệu, lộ trình, tab "Sinh bằng AI". Sửa lỗi giờ lệch 7 tiếng do SQLite bỏ múi giờ | Test: 110/110 pytest (Gemini giả đọc prompt thật, gài lỗi trích dẫn / đáp án / sập API); bộ sinh bản nháp Python so với JS trên 4 tài liệu mẫu × 6 cấu hình → giống hệt; frontend build + 77/77 Vitest; E2E Chrome headless với backend: HR tải 3 tài liệu → sinh lộ trình → gửi duyệt → Reviewer phát hành → nhân viên mở học phần, không lỗi console. Chưa chạy với Gemini thật (chưa có key) | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `frontend/src/utils/chunker.js`, `backend/app/ingestion/chunker.py`, `frontend/tests/chunker.test.js`, `backend/tests/test_ingestion.py` | Loại hỗ trợ: rà soát dữ liệu + sửa lỗi. Đánh giá `sample_documents/` và `role_matrix/` nhánh `feat/le-thi-kieu-duyen` bằng pipeline backend. Phát hiện chunker nhận nhầm dòng PDF bị ngắt ("5 days until 31 March…") là tiêu đề đánh số, làm cắt đôi mục §6 của DOC-02 (ca kiểm thử mâu thuẫn). Sửa: tiêu đề đánh số phải bắt đầu bằng chữ in hoa, ở cả frontend và backend | Test: 112/112 pytest, 78/78 Vitest; so khớp chunker và bộ sinh bản nháp Python với JS → vẫn giống hệt; quét lại 16 file mẫu → không còn tiêu đề nhận nhầm | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `sample_documents/DOC-11…DOC-20_*.pdf`, `sample_documents/source/*.md`, `sample_documents/source/build_pdfs.mjs`, `sample_documents/README_PHASE2.md` | Loại hỗ trợ: soạn tài liệu mẫu + công cụ. Viết 10 tài liệu còn thiếu của danh mục (SOP onboarding, sổ tay vận hành chi nhánh, 3 bộ mô tả công việc, FAQ, 4 tài liệu kiểm thử: mâu thuẫn, prompt injection, quy định hết hạn, ngoại lệ phòng ban), số liệu khớp DOC-01…10; lấp khoảng trống R040/R048 của ma trận vai trò; build PDF bằng Chrome headless | Mỗi PDF 3–5 trang; chạy qua pipeline backend: trích xuất + chia chunk không nhận nhầm tiêu đề, mỗi tài liệu sinh 5 câu hỏi và 2–3 nhiệm vụ; DOC-18 bị gắn cờ đủ 9/9 luật ở 4 chunk, 3 biến thể né lọc không bị bắt (đúng thiết kế); đáp án kiểm thử ghi trong README_PHASE2. Nội dung là dữ liệu giả lập cho công ty hư cấu, cần nhóm QA đọc duyệt | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `frontend/src/utils/chunker.js`, `backend/app/ingestion/chunker.py`, tests, `sample_documents/README_PHASE2.md` | Loại hỗ trợ: kiểm thử dữ liệu + sửa lỗi. Chạy toàn bộ DOC-01…20 qua API vào DB mới: tải lên, vòng đời phiên bản, cờ injection, sinh lộ trình cho 10 vị trí, kiểm định phía server, đối chiếu ma trận vai trò, luồng duyệt. Phát hiện lỗi chunker thứ hai ("31 December. This rule…" bị coi là tiêu đề, cắt DOC-02 §6); siết luật tiêu đề đánh số: không có dấu kết thúc câu bên trong, tối đa 80 ký tự sau số (tiêu đề thật dài nhất trong 290 tiêu đề của 20 tài liệu là 68 ký tự) | Kết quả: 20/20 tài liệu xử lý xong, 0 tiêu đề nhận nhầm trên 285 chunk; DOC-02 obsolete, DOC-19 expired; DOC-18 loại đúng 4 chunk, 0 injection lọt vào lộ trình; 10/10 lộ trình không lỗi chặn, 100% trích dẫn khớp; 114/114 pytest, 79/79 Vitest; chunker và bộ sinh bản nháp Python vẫn giống hệt JS | Phạm Tấn Tài |
| 2026-09-26 | Claude Code (Claude Opus 5.5) | `backend/app/ingestion/validation.py`, `frontend/src/{data/company,utils/documentValidation}.js`, `frontend/src/pages/hr/Documents.jsx`, `frontend/src/locales/*`, `backend/tests/test_documents_api.py`, `frontend/tests/documentValidation.test.js` | Loại hỗ trợ: sinh code. Nhận thêm đuôi `.markdown` (định dạng tuỳ chọn trong đề bài): quy về `md` ngay khi đọc đuôi file nên trích xuất, xem trước và trích dẫn dùng chung một loại; ô chọn file và thông báo lỗi liệt kê cả `.markdown` | Test: 1 test API (file `.markdown` lưu thành `md`, chia đúng 2 chunk), 3 Vitest (đuôi file, bản nháp tải lên, danh sách định dạng trong lỗi); 82/82 Vitest | Phạm Tấn Tài |
| 2026-09-26 | Claude Code (Claude Opus 5.5) | `sample_documents/source/DOC-{11,12,13,14,15,16,20}_*_v*.md`, `sample_documents/DOC-*_v{1.1,1.2,2.0}.pdf`, `sample_documents/source/build_pdfs.mjs`, `sample_documents/README_PHASE2.md`, `backend/tests/test_sample_versions.py` | Loại hỗ trợ: soạn dữ liệu + công cụ. 10 phiên bản chính sách mới (chỉ tiêu ≥ 10 policy-version changes): mỗi bản là tài liệu đầy đủ có `supersedes` và mục Revision History ghi giá trị cũ; thay đổi kéo theo tài liệu khác phát hành cùng ngày để các bản đang hiệu lực không mâu thuẫn; DOC-20 v1.2 hiệu lực 2027 để test `upcoming`. Script build nhận file nguồn theo phiên bản | Mỗi thay đổi áp đúng 1 chỗ (script dừng nếu không khớp); quét DOC-01…20: giá trị cũ chỉ còn trong bản cũ; 20 PDF qua pipeline backend: 3–5 trang, heading giống bản v1.0, bảng Revision History không bị nhận nhầm là heading; test vòng đời ở 26/09/2026 và 01/01/2027, chunk trích đúng giá trị theo phiên bản. Nội dung là dữ liệu giả lập, cần nhóm QA đọc duyệt | Phạm Tấn Tài |
| 2026-09-26 | Claude Code (Claude Opus 5.5) | `role_matrix/role_matrix.csv`, `role_matrix/README.md`, `backend/app/{models/organization,services/role_matrix}.py`, migration `employee profile role matrix` (chưa commit), `backend/tests/test_role_matrix_dataset.py` | Loại hỗ trợ: soạn dữ liệu + sinh code. Mở rộng ma trận vai trò từ 80 lên 203 dòng (156 yêu cầu khác nhau, 188 bắt buộc, 133 yêu cầu theo chức vụ) từ bản đang hiệu lực của DOC-11…16, DOC-20; thay 2 dòng giữ chỗ R040/R048; thêm cột `Source_Version` và `Scope`; bộ import đọc và kiểm tra 2 cột mới | Test: chỉ tiêu tối thiểu; import 203 dòng không lỗi; không trích tài liệu test; 125 dòng trích DOC-11…20 đều trỏ tới mục có thật trong đúng phiên bản đang hiệu lực; toàn bộ pytest đạt | Phạm Tấn Tài |
| 2026-09-26 | Claude Code (Claude Opus 5.5) | `backend/alembic/env.py`, `backend/tests/test_database.py` | Loại hỗ trợ: sửa lỗi. Migration mới thất bại trên mọi DB SQLite đã có dữ liệu: batch mode dựng lại bảng `users` nên phải xoá bảng cũ, mà việc xoá bị chặn khi các bảng khác (audit log, tài liệu…) đang có khoá ngoại trỏ tới `users`; lỗi giữa chừng còn để lại bảng rác vì pysqlite để DDL ngoài transaction. Sửa: khi chạy migration SQLite thì tắt kiểm tra khoá ngoại, tự quản `BEGIN` để mỗi lần chạy hoặc thành công hết hoặc không thay đổi gì, cuối cùng chạy `PRAGMA foreign_key_check` | Test: nâng cấp DB có sẵn dữ liệu giữ nguyên các dòng tham chiếu; migration lỗi giữa chừng rollback sạch; 2 test này fail khi gỡ bản sửa; 304/304 pytest; nâng cấp DB local thật (20 tài liệu, 285 chunk) thành công, `foreign_key_check` rỗng | Phạm Tấn Tài |
| 2026-09-26 | Claude Code (Claude Opus 5.5) | `backend/app/{genai_pipeline/{types,local_draft,generator,grounding,schemas}.py,genai_pipeline/prompts/v1.1/*,services/{paths,path_checks,path_workflow,role_matrix}.py,api/routes/catalog.py,schemas/{paths,catalog}.py,models/learning_path.py}`, migration `path duration`, `frontend/src/{pages/hr/CreatePath.jsx,components/path/PathContent.jsx,pages/employee/ModuleView.jsx,utils/{pathGenerator,pathChecks}.js,data/company.js,contexts/PathsContext.jsx,services/{apiMappers,pipelineService}.js,locales/*,styles/features.css}`, tests | Loại hỗ trợ: sinh code + thiết kế, theo góp ý review màn hình tạo lộ trình. (1) Tài liệu bắt buộc theo Role Requirement Matrix: API `GET /job-positions/{id}/required-sources` quy mỗi mã về bản đang hiệu lực; giao diện chọn sẵn và khoá, server trả `err_mandatory_sources` nếu bỏ sót; tài liệu bắt buộc chưa có trong kho được báo cho Reviewer. (2) Độ dài lộ trình 1 tuần / 30 / 90 ngày (SRS Step 13): cắt mẫu giai đoạn, mốc xa dồn vào giai đoạn cuối, lưu `duration_days`, AI được báo giai đoạn của học phần. (3) Nhiệm vụ bắt buộc có tiêu chí hoàn thành + nguồn: prompt v1.1, grounding loại nhiệm vụ thiếu tiêu chí hoặc có số liệu không có trong chunk, kiểm định Python/JS báo lỗi chặn `flow_task_no_criteria` | Test: 267/267 pytest (thêm 19 test: ma trận, bản cũ không thay được bản bắt buộc, sinh lại, độ dài, chuyển giai đoạn, FakeLLM gài thiếu tiêu chí / bịa hạn chót, chặn duyệt); 89/89 Vitest; bộ sinh bản nháp Python và JS giống hệt trên 16 cấu hình (2 mục đích × 2 mức × 4 độ dài); E2E Chrome headless với backend thật: vị trí CS khoá 11 tài liệu bắt buộc, bấm vào không bỏ chọn được, lộ trình 1 tuần chỉ có Ngày 1 / Tuần 1, 23/23 nhiệm vụ có tiêu chí và nguồn, không lỗi console. Chưa chạy với Gemini thật | Phạm Tấn Tài |
| 2026-09-26 | Claude Code (Claude Opus 5.5) | `backend/app/genai_pipeline/{requirements,generator,grounding,schemas,types}.py`, `prompts/v1.1/{system,module,quiz}.md`, `backend/app/services/{paths,path_checks,role_matrix}.py`, `frontend/src/{utils/pathChecks.js,components/path/{PathContent,GenerationReport}.jsx,pages/employee/ModuleView.jsx,locales/*,styles/features.css}`, tests | Loại hỗ trợ: sinh code + thiết kế prompt, theo góp ý "AI thiết kế lộ trình như chuyên gia". Luật sư phạm trong system prompt (cấu trúc do luật quyết định, dạy trước rồi mới kiểm tra, độ khó theo mức, ma trận là đề bài); đưa ma trận yêu cầu của vị trí và cách đánh giá vào prompt từng học phần; lời gọi sinh câu hỏi nhận danh sách bài vừa dạy; `learning_objectives` trong structured output; grounding loại mục về nội dung chưa dạy; Python gắn `requirement_ids` theo mục tài liệu và báo độ phủ; kiểm định Python/JS có lỗi chặn `flow_untaught_item` | Test: 279/279 pytest (thêm 12: khớp mục ↔ yêu cầu, nội dung prompt, gắn mã và độ phủ, bản nháp dùng yêu cầu làm mục tiêu, FakeLLM gài nội dung chưa dạy, kiểm định khi HR xoá bài, API đưa ma trận vào prompt); 91/91 Vitest; sinh thật lộ trình CS 30 ngày trên 11 tài liệu mẫu: 15/15 yêu cầu bắt buộc được dạy, 0 mục chưa dạy, báo cáo chỉ ra 7 yêu cầu chưa được kiểm tra (bản nháp theo luật). Chưa chạy với Gemini thật | Phạm Tấn Tài |
| 2026-09-26 | Claude Code (Claude Opus 5.5) | `backend/app/services/generation_jobs.py`, `backend/app/{genai_pipeline/{generator,grounding,schemas}.py,genai_pipeline/prompts/v1.1/quiz.md,services/paths.py,api/routes/paths.py,schemas/paths.py}`, `backend/tests/{conftest,test_generation_jobs,test_pedagogy}.py`, `frontend/src/{components/path/{GenerationProgress,PathActions}.jsx,pages/hr/CreatePath.jsx,contexts/PathsContext.jsx,locales/*,styles/features.css}`, `frontend/tests/generationProgress.test.js` | Loại hỗ trợ: sinh code + thiết kế UX. Màn hình tiến độ khi AI soạn lộ trình, theo tiến độ thật (job chạy nền + polling): phần trăm, đồng hồ, dòng thời gian 6 bước, trạng thái từng học phần. Sửa lỗi phát hiện khi chạy thật với Gemini: (1) test đọc GEMINI_API_KEY thật trong `.env` và gọi mạng, tiêu hết quota Free tier trong ngày; (2) câu hỏi bản nháp dùng khi AI hỏng bị gắn cờ "chưa dạy", nay được trỏ lại đúng chunk và lọc theo nội dung đã dạy; (3) prompt câu hỏi yêu cầu phương án giữ ngôn ngữ tài liệu. Tắt cảnh báo AFC của SDK | Test: 288/288 pytest (thêm 9: trạng thái job, job qua API, học phần lỗi hiện trong dòng thời gian, lỗi nghiệp vụ, job riêng tư, câu hỏi dự phòng chỉ về nội dung đã dạy); 94/94 Vitest; E2E Chrome với Gemini thật (`gemini-3.5-flash-lite`) cho lộ trình CS 30 ngày: dòng thời gian cập nhật từng học phần, không lỗi console; sau khi sửa: 0 lỗi chặn, 140/140 mục trích đúng, câu hỏi bị loại vì đáp án ngoài câu trích giảm từ 33 xuống 15. 3 học phần gặp 429 do hết quota miễn phí | Phạm Tấn Tài |
| 2026-09-26 | Claude Code (Claude Opus 5.5) | `backend/app/genai_pipeline/{client,generator}.py`, `backend/app/core/config.py`, `backend/.env.example`, `backend/tests/test_gemini_client.py`, `frontend/src/components/path/{GenerationProgress,GenerationReport}.jsx`, `frontend/src/locales/*`, `frontend/tests/generationProgress.test.js` | Loại hỗ trợ: sinh code, theo góp ý so sánh với dự án LearnPath. (1) Bóc JSON khỏi khối markdown / chữ thừa trước khi kiểm tra schema. (2) Không dùng lộ trình giả; giữ bản nháp theo luật dựng từ tài liệu, thêm mã lỗi QUOTA_EXCEEDED / MODEL_UNAVAILABLE, ngừng gọi khi mọi model hết quota, thông báo lịch sự trên màn hình tiến độ. (3) Tự chuyển model theo `GEMINI_FALLBACK_MODELS` khi gặp 404/429, không dựa vào `models.list()` (danh sách liệt kê cả model đã đóng với tài khoản mới) | Test: 296/296 pytest (thêm 8 test client với SDK giả: JSON trong khối markdown, model bị khai tử, hết quota chuyển model, hết quota toàn bộ không gửi request, lỗi 400 không đổi model); 95/95 Vitest; kiểm tra key thật: liệt kê model và 1 lời gọi thành công | Phạm Tấn Tài |
| 2026-09-26 | Claude Code (Claude Opus 5.5) | `backend/app/genai_pipeline/{relevance,requirements,generator,schemas,types}.py`, `prompts/v1.1/system.md`, `backend/app/ingestion/chunker.py` (hàm `outline`), `backend/app/services/paths.py`, `backend/tests/{test_relevance,test_pedagogy,test_path_requirements}.py`, `tests/fake_llm.py`, `frontend/src/components/path/{GenerationReport,GenerationProgress,PathContent}.jsx`, `frontend/src/locales/*`, `frontend/src/styles/features.css` | Loại hỗ trợ: sinh code, theo checklist "GenAI pipeline". (1) Chỉ gửi AI các mục tài liệu của đúng vị trí: bỏ mục mà tiêu đề (hoặc tiêu đề cha, lấy từ dàn ý đọc lại trong file) thuộc vị trí khác, `[ROLE-SPECIFIC: X]` khác, hoặc `[EXCEPTION]` của phòng ban khác; mục ma trận trích luôn giữ. (2) AI tự gắn cờ chunk chứa lệnh lạ (`suspicious_chunk_ids`), chunk đó bị loại khỏi nội dung và báo cho Reviewer. (3) `mandatory` và `source_sections` cho từng học phần, `mandatory` cho từng mục | Test: 313/313 pytest (thêm 17: luật nhận tiêu đề theo vị trí, mục con theo mục cha, ma trận ghi đè, tài liệu cả văn bản, lọc khi sinh, API đọc dàn ý từ file PDF thật, AI gắn cờ, cờ bắt buộc); 95/95 Vitest; chạy trên dữ liệu thật (không gọi Gemini): lộ trình CS bỏ 13 mục của vị trí khác (DOC-04/12/13/14/20), 0 bài học về vai trò khác, 15/15 yêu cầu bắt buộc vẫn được dạy | Phạm Tấn Tài |
| 2026-09-26 | Claude Code (Claude Opus 5.5) | `backend/app/genai_pipeline/generator.py`, `frontend/src/components/path/{Badges,GenerationReport}.jsx`, `frontend/src/locales/*`, `backend/tests/test_generation.py` | Loại hỗ trợ: chạy thử toàn hệ thống + sửa lỗi giao diện. Chạy thật với Gemini cho lộ trình CS: 11 học phần, lọc 13 mục của vị trí khác, chuỗi model dự phòng chuyển 3.5-flash-lite → flash-lite-latest → flash-latest khi hết quota/quá tải, dừng gọi khi không còn model. Sửa: học phần dùng bản nháp vì lỗi AI không còn bị gắn nhãn "chưa nối Gemini" (nay là "bản nháp dự phòng"); báo cáo ghi số bài / nhiệm vụ / câu hỏi cho cả học phần dự phòng | Kết quả chạy thật: 6/11 học phần do Gemini viết (5 dùng bản nháp vì quota/503), 149/149 mục trích đúng tài liệu, 0 lỗi chặn, 15/16 yêu cầu được dạy, không lỗi console; 313/313 pytest, 95/95 Vitest | Phạm Tấn Tài |
| 2026-09-26 | Claude Code (Claude Opus 5.5) | `documentation/AI_WRITING_SIGNS.html`, `Rules/Rules` (mục 6), `README.md` | Loại hỗ trợ: soạn tài liệu + luật nhóm. Tóm tắt và dịch trang Wikipedia:Signs of AI writing (bản 26/09/2026, CC BY-SA 4.0) thành 47 dấu hiệu chia 10 nhóm, mỗi dấu hiệu có từ cần để ý, ví dụ nguyên văn và cách áp dụng cho repo; thêm bảng cụm tương đương tiếng Việt (nhóm tự bổ sung, ghi rõ không có trong nguồn) và 2 lệnh `git grep` rà trước commit. Rules mục 6 chuyển các dấu hiệu thành luật cho tài liệu, commit, AI_USAGE, chuỗi giao diện và prompt Gemini | Đối chiếu từng mục với wikitext gốc; 22 mã dấu hiệu Rules trích dẫn đều có trong file HTML; 2 lệnh grep chạy trên repo không ra kết quả và bắt đúng 4/4 dòng rác mẫu; Chrome headless: 1366px và 390px không tràn ngang, lọc không dấu hoạt động, chế độ tối, không lỗi console. Prompt v1.1 chưa có yêu cầu văn phong, sẽ thêm khi tạo phiên bản prompt mới | Phạm Tấn Tài |
| 2026-09-26 | Claude Code (Claude Opus 5.5) | `backend/app/{api/routes/invite.py,core/email.py,core/config.py,models/invitation.py,schemas/invite.py,schemas/auth.py}`, migration `invitation_tokens`, `backend/tests/{test_invite,conftest,test_path_requirements}.py`, `backend/.env.example`, `frontend/src/pages/{auth/SelfRegister,hr/Invites}.jsx`, `frontend/src/{locales/*,styles/{features,index}.css,components/Icons.jsx,utils/injectionScan.js,services/documentStore.js}`, `frontend/tests/injectionScan.test.js`, `backend/README.md`, `documentation/FRONTEND_FLOWS.md` | Loại hỗ trợ: rà soát toàn dự án trước khi đưa lên GitHub theo Rules mục 1 và 6. Phần lớn comment đã đúng kiểu "vì sao" nên giữ nguyên; dọn phần mời nhân viên mới: bỏ `except Exception: pass`, comment mô tả "làm gì", đường kẻ trang trí, emoji, chuỗi tiếng Việt viết cứng trong app song ngữ; email không còn gửi mật khẩu, escape tên trước khi đưa vào HTML, bỏ câu hứa sai ("AI đang tạo lộ trình"), bỏ hàm `send_path_ready` và schema `EmployeeCreate/Update/Profile` không dùng; `created_by` hết mâu thuẫn NOT NULL + `ON DELETE SET NULL`; mật khẩu giới hạn theo byte (72 byte của bcrypt) thay vì trả 500; test không bao giờ gửi email thật. Frontend: 2 trang dùng khoá dịch (64 khoá en/vi), báo cần backend thay vì tải mãi, CSS đăng ký chỉ áp dụng trong `.register-card`; sửa `items-align` sai trong CSS, xoá mục CSS rỗng, 3 icon và 3 comment trỏ tới file không còn tồn tại; menu tiếng Anh viết hoa kiểu câu; sửa 22 lỗi ruff | Lỗi phát hiện khi chạy thật: form đăng ký gửi `beginner` trong khi backend nhận `Beginner`, nên trước đây mọi lần đăng ký đều bị 422; CSS đăng ký làm lệch ô nhập trang đăng nhập. Test: 9 test mới cho lời mời (tạo, phân quyền, đăng ký một lần rồi đăng nhập, email trùng, mật khẩu quá 72 byte, thu hồi, escape HTML, SMTP lỗi vẫn tạo được link, email chào mừng không chứa mật khẩu); 322/322 pytest, ruff sạch, 95/95 Vitest, build; E2E Chrome với backend thật: HR tạo lời mời (vi/en) → nhân viên đăng ký → dùng lại link báo hết hạn → nhân viên đăng nhập → HR thấy *Đã đăng ký*; quét dấu hiệu văn AI trên toàn repo không còn kết quả | Phạm Tấn Tài |
| 2026-09-26 | Claude Code (Claude Opus 5.5) | `sample_documents/DOC-21…DOC-28_*` (PDF, DOCX, MD, TXT, CSV), `sample_documents/source/DOC-21…28_*`, `sample_documents/source/build_other_formats.py`, `sample_documents/source/build_pdfs.mjs`, `sample_documents/README_PHASE2.md` (mục 9), `backend/app/services/document_catalog.py`, `frontend/src/data/company.js`, `backend/tests/factories.py` | Loại hỗ trợ: soạn tài liệu mẫu + công cụ. 8 tài liệu mới cho các phòng ban chưa có hoặc có ít tài liệu: Marketing (DOC-21 DOCX, DOC-22 PDF), Operations (DOC-23 PDF, DOC-24 DOCX), Data (DOC-25 MD, DOC-26 CSV 40 chỉ số), Customer Support (DOC-27 TXT), Finance (DOC-28 PDF). Số liệu lấy từ bản đang hiệu lực của DOC-01…20 kèm nguồn; quy định sắp đổi chỉ dẫn nguồn, không chép lại. Script build DOCX/TXT/MD/CSV từ nguồn Markdown; thêm 8 mã vào danh mục frontend và backend; đã tải cả 8 vào kho tài liệu qua API | Độ dài: PDF 4 trang (PyMuPDF), DOCX 3 trang (Microsoft Word), MD/TXT/CSV 4 trang khi mở bằng Word. Pipeline backend: 8/8 xử lý xong, 0 heading nhận nhầm, 0 cờ injection; bộ sinh bản nháp tạo được bài học và câu hỏi (CSV chỉ có bài học). Đã sao lưu DB trước khi tải lên. 322/322 pytest, ruff sạch, 95/95 Vitest, build. Nội dung là dữ liệu giả lập cho công ty hư cấu, cần nhóm QA đọc duyệt | Phạm Tấn Tài |
| 2026-09-26 | Claude Code (Claude Opus 5.5) | `frontend/src/pages/hr/CreatePath.jsx`, `frontend/src/contexts/PathsContext.jsx`, `frontend/src/components/path/{PathActions,GenerationReport}.jsx`, `frontend/src/styles/features.css`, `frontend/src/locales/*`, `backend/app/{schemas,services}/paths.py`, `backend/tests/test_path_requirements.py`, `backend/README.md`, `documentation/FRONTEND_FLOWS.md` | Loại hỗ trợ: sửa code theo yêu cầu. Tài liệu bắt buộc theo ma trận vẫn được chọn sẵn nhưng HR bỏ chọn được: tài liệu bị bỏ có viền cam, dòng cảnh báo liệt kê mã đã bỏ kèm nút *Chọn lại đủ tài liệu bắt buộc*. Server chỉ nhận thiếu tài liệu bắt buộc khi request có `allow_missing_mandatory`; mã bị bỏ lưu ở `generation.mandatory_omitted` và audit log, Reviewer thấy cảnh báo ở tab Sinh bằng AI và kết quả kiểm định; sinh lại giữ quyết định bỏ | Test: 1 test mới (bỏ khi có xác nhận → 201, lưu mã bỏ, cảnh báo kiểm định, audit; sinh lại không xác nhận → 422, có xác nhận → 200); 323/323 pytest, ruff sạch; 95/95 Vitest; build; khoá en/vi khớp 747/747. Chrome headless (không bấm sinh, không ghi DB): vị trí CS 11 tài liệu bắt buộc → bỏ 1 rồi 2 tài liệu, số tài liệu chọn 11 → 10 → 9, cảnh báo liệt kê đúng mã → nút chọn lại đưa về 11; *Chọn tất cả* cũng chọn lại; tiếng Anh hiển thị đúng; không lỗi console | Phạm Tấn Tài |
| 2026-09-26 | Claude Code (Claude Opus 5.5) | `documentation/DESIGN_PATH_ASSIGNMENT.md`, `README.md` | Loại hỗ trợ: phân tích + thiết kế, chưa code. Đối chiếu đặc tả "hai loại lộ trình, duyệt, gán, thực hiện" với hệ thống; đề xuất mở rộng `enrollments`, bảng `notifications`, luật tự gán Hội nhập, gán đích danh Thăng tiến kèm hạn, API, quyền, giao diện, kiểm thử, 6 câu hỏi cần nhóm quyết, thứ tự triển khai. Giữ tên trạng thái hiện tại và bước bản nháp theo lựa chọn của nhóm | Đối chiếu từng mục với code: bảng `enrollments` có nhưng chưa có API, tiến độ đang ở `localStorage`, `approve` chỉ nhận phòng ban/vị trí, không có API danh sách nhân viên, `audit_logs.actor_id` bắt buộc; kiểm tra thật qua API rằng dữ liệu lộ trình gửi xuống có `answer` của câu hỏi (Q6); số liệu backfill lấy từ DB hiện tại (1 lộ trình đã phát hành, 2 nhân viên) | Phạm Tấn Tài |
| 2026-09-26 | Claude Code (Claude Opus 5.5) | `backend/app/{models/{enrollment,enums,__init__}.py,services/{enrollments,progress,visibility,paths}.py,schemas/enrollments.py,api/routes/{enrollments,invite}.py,api/router.py}`, migration `enrollment assignment`, `backend/tests/{test_enrollments,test_paths_api}.py`, `frontend/src/{contexts/EnrollmentContext.jsx,hooks/useMyPaths.js,services/apiMappers.js,pages/employee/{ModuleView,MyPaths}.jsx,locales/*}`, `documentation/{DESIGN_PATH_ASSIGNMENT,FRONTEND_FLOWS}.md`, `backend/README.md` | Loại hỗ trợ: sinh code theo thiết kế đã duyệt (bước 1–2, Q1 và Q2). Bản ghi gán lộ trình ở server (mở rộng `enrollments`), tự gán Hội nhập khi phát hành và khi nhân viên đăng ký qua link mời, bỏ qua người đã hoàn thành hội nhập (Q1), gán mọi lộ trình khớp (Q2), hạn hoàn thành, quyền xem theo bản ghi gán, API tiến độ (server chấm bài, tính hoàn thành bằng bản chép Python của `progress.js`), thu hồi rút bản ghi đang học; frontend gọi API thay cho `localStorage` khi có backend. Tạm thời lộ trình Thăng tiến vẫn gán theo đích phát hành cho tới khi có gán thủ công | Test: 9 test mới (Q1, Q2, Company-wide, Thăng tiến, hạn, đăng ký qua link mời, assigned → in_progress → completed, quyền, thu hồi); 332/332 pytest, ruff sạch; migration nâng cấp/hạ cấp trên bản sao DB thật, `alembic check` khớp, dữ liệu giữ nguyên; Python và JS cho cùng kết quả trên 500 trường hợp ngẫu nhiên; 95/95 Vitest, build. E2E Chrome trên bản sao DB (không đụng DB thật): phát hành 2 lộ trình cho Engineering và vị trí support-engineer → Alex thấy cả 2, hạn 20/12/2026; đọc 1 bài → server ghi `in_progress`, thẻ hiện *Đang học*; Minh (đã hoàn thành hội nhập) không thấy lộ trình nào; không lỗi console. Đã sao lưu và nâng cấp DB thật (28 tài liệu, 9 lộ trình giữ nguyên) | Phạm Tấn Tài |
| 2026-09-26 | Claude Code (Claude Opus 5.5) | `backend/app/{models/enums.py,services/enrollments.py,schemas/enrollments.py,api/routes/enrollments.py}`, migration `enrollment self source`, `backend/tests/test_enrollments.py`, `frontend/src/{pages/employee/{Explore,Dashboard,MyPaths}.jsx,contexts/{EnrollmentContext,PathsContext}.jsx,layouts/RoleLayout.jsx,App.jsx,components/Icons.jsx,styles/features.css,locales/*}`, tài liệu | Loại hỗ trợ: sinh code theo mô tả trải nghiệm của nhóm. Trang chủ nhân viên: *Lộ trình đang học* có thanh tiến độ, nút *Bắt đầu học* / *Tiếp tục học*, hạn, quá hạn, xếp theo hạn; trạng thái đang tải thay cho "chưa có lộ trình" lúc mới đăng nhập. Menu *Khám phá lộ trình*: lộ trình của phòng ban (cả của các vị trí trong phòng), xem trước dàn ý không có nội dung và đáp án, tự đăng ký (`source = self`, không có hạn, nhãn *Tự chọn*) | Test: 3 test mới (danh sách khám phá gồm lộ trình chưa được giao, xem trước không lộ đáp án, tự đăng ký / đăng ký trùng 409 / phòng khác 404 / HR 403); 335/335 pytest, ruff sạch; migration nâng cấp/hạ cấp trên bản sao DB, `alembic check` khớp; 95/95 Vitest, build, khoá en/vi khớp. E2E Chrome trên bản sao DB: Alex đăng nhập → trang chủ có lộ trình được giao 0%, hạn 20/12/2026, nút *Bắt đầu học* mở học phần đầu; Khám phá thấy 2 lộ trình, xem trước 5 giai đoạn / 12 học phần, đăng ký → nhãn *Tự chọn*, trang chủ có 2 lộ trình; lấy mẫu mỗi 30 ms sau đăng nhập không còn thoáng hiện "Chưa có lộ trình"; không lỗi console. Đã sao lưu và nâng cấp DB thật | Phạm Tấn Tài |
| 2026-09-26 | Claude Code / Antigravity | `src/**`, `backend/app/services/path_checks.py`, `backend/app/db/seed.py`, `tests/test_adversarial.py`, `README.md` | Loại hỗ trợ: rà soát đề bài SRS, sửa lỗi duplicate detection, đồng bộ bộ core `src/` chính gốc, sửa seed demo certificate và kiểm thử toàn diện 545/545 tests | Đã đối chiếu 66 yêu cầu chức năng SRS Mục 1.6 và 18 deliverables Mục 1.10; cài đặt thuật toán so khớp SequenceMatcher ngưỡng 0.85; xác nhận 25 test adversarial và 362 test backend pass 100% | Châu Quốc Lâm Phong |
