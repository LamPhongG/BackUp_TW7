# AI Usage Declaration Log — SkillSprint AI
**Dự án:** SkillSprint AI – Dual-Pipeline AI Document Verification System  
**Cuộc thi:** TechWiz 7 – Generative AI Powerplay Track  
**Đội thi:** Four Angry Birds  

> **Tuyên bố minh bạch:** Tài liệu này ghi nhận toàn bộ các hoạt động sử dụng AI hỗ trợ trong quá trình phát triển dự án theo đúng quy chế cuộc thi TechWiz 7. Toàn bộ mã nguồn và tài liệu đều đã qua quy trình rà soát thủ công của các thành viên, refactor tên biến và loại bỏ các thành phần AI boilerplate.

---

## I. NHẬT KÝ CHI TIẾT THEO CÁC PHASES (PHASE 1 – 5)

| Ngày | Công cụ AI | Module / File ảnh hưởng | Mục đích sử dụng | Kiểm thử & Xác minh bởi con người | Người xác nhận |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **2026-09-24** | Claude / Gemini | Khởi tạo cấu trúc dự án & WBS | Tạo khung thư mục ban đầu theo kiến trúc Dual-Pipeline | So sánh với đặc tả đề bài và phân bổ RACI 4 thành viên | Team Lead |
| **2026-09-24** | Claude / Gemini | `src/document_processing/pdf_reader.py` | Viết hàm đọc PDF với PyMuPDF, trích xuất text từng trang | Kiểm tra exception handling, docstring chuẩn, kiểm tra rò rỉ bộ nhớ | Châu Quốc Lâm Phong |
| **2026-09-24** | Claude / Gemini | `src/document_processing/chunker.py` | Viết logic chunking chia text theo heading/section | Kiểm tra regex pattern, logic flush chunk cuối, giới hạn MAX_CHUNK | Châu Quốc Lâm Phong |
| **2026-09-24** | Claude / Gemini | `src/document_processing/docx_reader.py` | Viết hàm đọc file DOCX với python-docx | Kiểm tra style-based heading detection, page break counting | Châu Quốc Lâm Phong |
| **2026-09-24** | Claude / Gemini | `src/document_validation/validator.py` | Viết module kiểm tra định dạng và kích thước file | Kiểm tra 3 loại exception, size bounds, extension whitelist | Châu Quốc Lâm Phong |
| **2026-09-24** | Claude / Gemini | `tests/test_document_processing.py` | Viết bộ unit test cho Ingestion & Chunker | Chạy pytest, kiểm tra các trường hợp biên (file thiếu, rỗng, sai định dạng) | Châu Quốc Lâm Phong |
| **2026-09-25** | Claude / Gemini | `src/genai_pipeline/gemini_client.py` | Viết client gọi Gemini API kèm exponential backoff retry | Bổ sung bắt lỗi `APITimeoutError`, ẩn API key qua `.env`, test retry 3 lần | Châu Quốc Lâm Phong |
| **2026-09-25** | Claude / Gemini | `src/genai_pipeline/response_schemas.py` | Định nghĩa schema Pydantic: `OnboardingPlan`, `Module`, `Task`, `Quiz` | Bắt buộc thuộc tính `source_citation` với `exact_quote` và `page_number` | Châu Quốc Lâm Phong |
| **2026-09-25** | Claude / Gemini | `src/prompt_templates/onboarding_plan_v1.txt` | Soạn prompt template có định dạng JSON schema | Thử nghiệm thủ công trên Google AI Studio, tinh chỉnh cấu trúc đầu ra | Châu Quốc Lâm Phong |
| **2026-09-25** | Claude / Gemini | `src/genai_pipeline/plan_generator.py` | Viết hàm sinh lộ trình onboarding từ chunks | Xử lý lỗi protobuf schema của Gemini bằng `response_mime_type: application/json` | Châu Quốc Lâm Phong |
| **2026-09-25** | Claude / Gemini | `src/genai_pipeline/quiz_generator.py` | Viết hàm sinh câu hỏi trắc nghiệm kiểm chứng | Kiểm tra tính liên kết giữa câu hỏi và trích dẫn gốc trong tài liệu | Châu Quốc Lâm Phong |
| **2026-09-26** | Claude / Gemini | `src/schemas/comparison_contract.py` | Thiết kế contract chung kết quả so sánh 2 pipeline | Thống nhất cấu trúc `ComparisonReport`, `ComparisonItem`, `HallucinationFlag` | Cả nhóm |
| **2026-09-26** | Claude / Gemini | `src/comparison_engine/engine.py` | Viết logic so sánh field-by-field song song | Tính match_score độc lập không dùng AI, đảm bảo tính khách quan | Châu Quốc Lâm Phong & Quỳnh Nhi |
| **2026-09-26** | Claude / Gemini | `src/hallucination_checks/detector.py` | Viết bộ phát hiện ảo giác (Hallucination Detector) | Nâng cấp thuật toán trích xuất số nguyên `\b\d+\b` để bắt lỗi sửa ngày phép | Châu Quốc Lâm Phong |
| **2026-09-26** | Claude / Gemini | `src/contradiction_checks/checker.py` | Viết bộ phát hiện mâu thuẫn chính sách nội bộ | Kiểm tra regex phát hiện mâu thuẫn thời hạn đổi mật khẩu và thông báo nghỉ | Quỳnh Nhi |
| **2026-09-26** | Claude / Gemini | `src/security/injection_filter.py` | Viết khiên lọc mã độc Prompt Injection | Quét regex chặn các chuỗi `SYSTEM OVERRIDE`, `DAN`, `ignore previous instructions` | Quỳnh Nhi & Lâm Phong |
| **2026-09-26** | Claude / Gemini | `tests/test_adversarial.py` | Xây dựng 11 kịch bản kiểm thử bẫy tấn công | Chạy 11/11 tests pass, xác nhận hệ thống tự hạ về `MANUAL_REVIEW_REQUIRED` | Lê Thị Kiều Duyên |
| **2026-09-26** | Claude / Gemini | `src/genai_pipeline/response_schemas.py` | Thêm RubricCriterionSchema (Step 24) và Difficulty levels (Step 25) | Kiểm tra Pydantic validation, tương thích ngược 100% với schema cũ | Châu Quốc Lâm Phong |
| **2026-09-26** | Claude / Gemini | `src/genai_pipeline/consistency_checker.py` | Viết engine kiểm tra và tính điểm GenAI Consistency (Step 44 & 45) | So khớp có trọng số: 40% nguồn trích dẫn, 35% danh mục chủ đề, 25% khối lượng task | Châu Quốc Lâm Phong |
| **2026-09-26** | Claude / Gemini | `src/genai_pipeline/selective_regenerator.py` | Viết engine Selective Regeneration sinh lại chọn lọc các module bị ảnh hưởng (Step 59) | Kiểm tra giữ nguyên các module không bị ảnh hưởng, chỉ cập nhật module chỉ định | Châu Quốc Lâm Phong |
| **2026-09-26** | Claude / Gemini | `tests/test_lam_phong_genai.py` | Bộ 6 unit test cho Rubric, Difficulty, Consistency, Selective Regeneration | Chạy `pytest`, 6/6 tests mới đạt 100% | Châu Quốc Lâm Phong |
| **2026-09-27** | Claude / Gemini | `src/document_processing/pdf_reader.py` | Gia cố xử lý edge cases (file scan, mã hóa, Unicode) | Thêm kiểm tra `pdf.is_encrypted`, ném `PDFReadError`, chuẩn hóa NFC | Châu Quốc Lâm Phong |
| **2026-09-27** | Claude / Gemini | `src/document_processing/docx_reader.py` | Bổ sung trích xuất dữ liệu Bảng (Tables) trong DOCX | Đọc toàn bộ các cell trong table, gộp nội dung dạng markdown table | Châu Quốc Lâm Phong |
| **2026-09-27** | Claude / Gemini | `hidden_test_ready/` | Xây dựng kịch bản và runner Hidden Test tự động | Chạy `run_hidden_test.py` trên tài liệu mới, xuất `hidden_test_report.json` | Châu Quốc Lâm Phong |
| **2026-09-27** | Claude / Gemini | `documentation/demo_script.md` | Biên soạn kịch bản video demo sản phẩm (05:00) | Bố cục 6 cảnh quay chi tiết, lời thoại và checklist quay cụ thể | Cả nhóm |
| **2026-09-28** | Claude / Gemini | `README.md` | Viết tài liệu hướng dẫn triển khai A – Z | Soát lỗi chính tả, xác nhận các lệnh chạy độc lập từ clone đến test | Cả nhóm |
| **2026-09-28** | Claude / Gemini | `.env.example` & `.gitignore` | Rà soát an toàn bảo mật và ẩn thông tin nhạy cảm | Kiểm tra `git log -S` đảm bảo không có API key hay secret nào bị lọt | Lê Thị Kiều Duyên |
| **2026-09-25** | Claude Code (Sonnet 5) | `backend/app/rule_pipeline/coverage.py`, `backend/app/services/paths.py`, `backend/tests/test_rule_pipeline.py` | Loại hỗ trợ: sinh code. Pipeline 2 (SRS Step 10, 28-30): Role Requirement Matrix tính động từ tài liệu đang active trong DB (category + phòng ban) thay vì `path.coverage` luôn `null` như trước đó; wiring vào create/regenerate/edit để không tin coverage do client/AI gửi | Test: 7 test mới, 121/121 pytest backend đạt, `ruff check` sạch | Đoàn Thị Quỳnh Nhi |
| **2026-09-25** | Claude Code (Sonnet 5) | `backend/app/services/{paths,path_workflow}.py`, `frontend/src/utils/pathWorkflow.js` | Loại hỗ trợ: sửa lỗi nghiêm trọng phát hiện sau khi merge `main`. Một phiên làm việc song song đã thêm auto-publish khi tạo path khiến HR tự duyệt được bài của chính mình (`path.approved_by_id = actor.id`) — vi phạm đúng điều `test_only_reviewers_decide` được viết để chặn; nới quyền `delete`/`regenerate` khi path đang `in_review`. Revert về đúng luồng draft → submit → Reviewer duyệt; bỏ lệnh gọi `calculate_role_coverage()` (ma trận hard-code theo role_id, có nguy cơ vi phạm luật "No Hard-Coded Output"/"Hidden Role" của SRS mục 1.8) vì bị `compute_coverage()` ghi đè ngay sau đó, tính 2 lần vô nghĩa | Test: 121/121 pytest backend đạt lại sau revert | Đoàn Thị Quỳnh Nhi |
| **2026-09-25** | Claude Code (Sonnet 5) | `backend/app/rule_pipeline/{coverage,matrix}.py`, `backend/tests/test_rule_pipeline.py` | Loại hỗ trợ: sinh code. Giữ lại dữ liệu từ khoá/chủ đề đã soạn tay theo role (`ROLE_REQUIREMENT_MATRIX`) làm lớp bổ sung: `keyword_topics()` trả `matchedKeyword` thật khi role đã biết, role lạ tự rơi về danh sách theo tài liệu bắt buộc (không đổi `score`/`requiredDocs` — vẫn tính động từ DB). Xoá hàm `calculate_role_coverage()` cũ không còn dùng | Test: 4 test mới, 125/125 pytest backend đạt | Đoàn Thị Quỳnh Nhi |
| **2026-09-26** | Claude Code (Sonnet 5) | `src/python_validation/{coverage_scorer,prerequisite_checker}.py`, `src/role_matrix/role_matrix.csv`, `tests/test_rule_engine.py`, `hidden_test_ready/run_hidden_test.py` | Loại hỗ trợ: sinh code. Hoàn thiện Phase 2 (`src/python_validation/`, `src/role_matrix/` trước đó chỉ có `.gitkeep`): đọc CSV Role Requirement Matrix (11 role), tính Coverage Score, kiểm tra thứ tự tiên quyết module — thuần Python, không import AI SDK. Nối vào Phase 3: `run_hidden_test.py` không còn hard-code `rule_data`, dòng "DevOps Engineer" trong CSV khớp đúng giá trị cũ để không đổi hành vi test hiện có | Test: 10 test mới (gồm 1 test tĩnh bằng `ast` xác nhận không import `google`/`openai`/`anthropic`); `tests/test_hidden_pipeline.py` (test cũ) vẫn pass không đổi; 64/64 pytest `src/` đạt, coverage 90% | Đoàn Thị Quỳnh Nhi |
| **2026-09-26** | Claude Code (Sonnet 5) | `requirements.txt`, `README.md`, `backend/.env.example` | Loại hỗ trợ: rà soát Phase 5 (dọn code, README, secret, coverage). Quét `src/`+`backend/app` không còn `# TODO`/`except Exception: pass`/bare `except:` (đã sạch từ trước). Quét `git log --all -p` không có secret thật bị lọt — **phát hiện và sửa 1 vấn đề thật**: `backend/.env.example` (file git theo dõi) đang có `JWT_SECRET` thật bị dán nhầm vào (chưa commit), đã revert về placeholder rỗng. Sửa `PyMuPDF==1.24.3` → `1.28.2` (bản cũ không build được trên Python 3.13, chặn người ngoài chạy README lần đầu); cập nhật số liệu test/coverage trong README | Test: cài `pip install -r requirements.txt` sạch trên venv mới với Python 3.13; 125/125 pytest backend (coverage 95%), 64/64 pytest `src/` (coverage 90%) | Đoàn Thị Quỳnh Nhi |
| **2026-09-26** | Claude Code (Sonnet 5) | `requirements.txt` | Loại hỗ trợ: dọn code (tiếp Phase 5). Phát hiện thêm lỗi build thứ 2 (`pydantic==2.7.1` không có wheel sẵn cho Python 3.13) → nâng lên `2.13.5` khớp bản backend. Xoá 11 dependency không ai dùng (`fastapi`, `uvicorn`, `sqlalchemy`, `psycopg2-binary`, `alembic`, `pandas`, `numpy`, `python-multipart`, `aiofiles`, `passlib`, `python-jose`) — xác nhận qua grep toàn bộ import trong `src/`+`tests/`, đây là phần kế hoạch app FastAPI gốc (WBS Phase 1) chưa từng được xây, đã bị thay bằng `backend/` riêng | Test: cài `pip install -r requirements.txt` sạch trên venv hoàn toàn mới, không còn lỗi build; 64/64 pytest `src/` đạt, coverage 90% | Đoàn Thị Quỳnh Nhi |
| **2026-09-26** | Claude Code (Sonnet 5) | `backend/app/rule_pipeline/{precedence,weak_areas}.py`, `backend/app/services/path_checks.py`, `backend/app/schemas/paths.py`, `src/comparison_engine/consistency.py`, `frontend/src/locales/{en,vi}.js` | Loại hỗ trợ: sinh code. Hoàn thiện 4 mục còn thiếu: (1) Step 34 Policy Precedence — `resolve_precedence()`/`sort_by_precedence()` xếp hạng tài liệu theo cấp bậc (Handbook/Policy/Compliance > SOP/Process Manual/Role Description > FAQ) rồi version mới hơn, nối vào `required_documents()` để `requiredDocs`/`topics` luôn liệt kê tài liệu thẩm quyền cao hơn trước; (2) Step 35 Duplicate Detection — `check_duplicates()` dùng `difflib.SequenceMatcher` so quiz/task trùng lặp giữa mọi module, thêm cảnh báo `reason_duplicate_content`; (3) Step 45 Consistency Scorer — `calculate_consistency_score(plan_a, plan_b)` so cấu trúc nghiệp vụ (tập doc_id, số module/task, câu hỏi trùng id) giữa 2 lần sinh, không so câu chữ; (4) Step 55-56 Weak-Area Detection — `analyze_weak_areas()` gộp quiz_attempts theo module, module <70% đúng → `weak_areas` + `manager_review_required=True` (chưa có endpoint nộp bài quiz gọi tới, hàm sẵn sàng cho sau) | Test: 20 test mới (5 precedence, 5 duplicate detection, 4 consistency, 6 weak-area); 141/141 pytest backend (coverage 95%), 68/68 pytest `src/` (coverage 90%); `ruff check` sạch; grep xác nhận không import AI SDK ở file mới | Đoàn Thị Quỳnh Nhi |

---

### Web application — `frontend/`, `backend/`, `sample_documents/` (Phạm Tấn Tài)

| Ngày | Công cụ AI | Module / File ảnh hưởng | Mục đích sử dụng | Kiểm thử & Xác minh bởi con người | Người xác nhận |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 2026-09-24 | Claude Code (Claude Opus 5.5) | `frontend/src/contexts/LanguageContext.jsx`, `frontend/src/locales/en.js`, `frontend/src/locales/vi.js` | Loại hỗ trợ: review code + sinh code. Rà soát logic chuyển đổi ngôn ngữ và viết lại i18n: `t()` có tham số và fallback tiếng Anh, `tv()`, `tNode()`, `pick()`, kiểm tra giá trị `localStorage` | Sửa: app crash khi `app_lang` không hợp lệ, key `close` bị trùng, thiếu `<html lang>`. Test: `npm run build`; render 31 route × `vi`/`en`/giá trị sai → 0 lỗi; đối chiếu key en/vi khớp 100% | Phạm Tấn Tài |
| 2026-09-24 | Claude Code (Claude Opus 5.5) | `frontend/src/pages/**`, `frontend/src/layouts/**`, `frontend/src/components/*` | Loại hỗ trợ: sinh code + sửa lỗi. Chuyển ngữ toàn bộ trang Employee / Manager / Admin / Auth; thêm nút đổi ngôn ngữ cho Manager và trang đăng nhập | Sửa lỗi: nút "Tiếp" bị cắt chữ, textarea AI Studio không đổi ngôn ngữ, quiz ghi cứng "2 / 3 đúng" (nay tính điểm thật), checklist đếm sai do làm tròn lên, link `/manager/settings` dẫn tới 404. Test: build + render toàn bộ route, quét chữ tiếng Anh còn sót ở chế độ VI | Phạm Tấn Tài |
| 2026-09-24 | Claude Code (Claude Opus 5.5) | `frontend/src/data/company.js`, `frontend/src/data/mock.js`, `frontend/src/utils/helpers.js`, `frontend/src/components/ValidationTag.jsx` | Loại hỗ trợ: sinh dữ liệu mẫu + code. Chuyển dữ liệu sang hồ sơ FourAngryBirds (10 vị trí, danh mục DOC-01…20); thêm giai đoạn Week 2 (SRS Step 13), trạng thái tiến độ (Step 54), đủ trạng thái kiểm định (mục 1.2), mức độ khó (Step 25) | Sửa: ngày tháng chuyển sang ISO và hiển thị theo locale; bỏ trạng thái cũ "At Risk". Test: build + render; kiểm tra badge/màu từng trạng thái | Phạm Tấn Tài |
| 2026-09-24 | Claude Code (Claude Opus 5.5) | `frontend/src/services/documentStore.js`, `frontend/src/contexts/DocumentsContext.jsx`, `frontend/src/utils/documentValidation.js`, `frontend/src/pages/admin/Documents.jsx`, `frontend/src/pages/admin/KnowledgeBase.jsx`, `frontend/src/pages/employee/Documents.jsx`, `frontend/src/pages/admin/AIStudio.jsx`, `frontend/src/styles/index.css` | Loại hỗ trợ: sinh code. Kho tài liệu cho HR: tải nhiều file, lưu IndexedDB, kiểm tra theo SRS Step 5 (định dạng qua magic bytes, dung lượng, file rỗng, trùng SHA-256, mã/phiên bản/ngày), quản lý phiên bản theo Step 8 | Sửa: side effect nằm trong updater `setState` của luồng upload cũ; bỏ phần giả lập "AI đang phân tích" và số chunk giả. Test: 26 unit test cho `documentValidation.js` (20 tên file của hồ sơ, PDF/DOCX giả, trùng hash/phiên bản, vòng đời phiên bản) → 26/26 đạt; build + render | Phạm Tấn Tài |
| 2026-09-24 | Claude Code (Claude Opus 5.5) | `frontend/` (xoá `update_fonts.*`, `layouts/AppShell.jsx`, `services/api.js`, 15 trang cũ ở `src/pages/*.jsx`), import thừa ở 7 file | Loại hỗ trợ: rà soát code chết. Tìm file và export không được import ở đâu | Đã kiểm tra từng file không còn được tham chiếu trước khi xoá. Test: build, 26 unit test, render 31 route sau khi dọn → không lỗi; quét import không dùng → 0 | Phạm Tấn Tài |
| 2026-09-24 | Claude Code (Claude Opus 5.5) | `frontend/README.md`, `AI_USAGE.md`, `.gitignore` | Loại hỗ trợ: soạn tài liệu. Mô tả công nghệ, cấu trúc, tính năng, kiểm thử, giới hạn; khai báo sử dụng AI; bỏ qua `node_modules/`, `dist/` | Đã đối chiếu nội dung README với code và kết quả test thực tế; ghi rõ các phần còn là dữ liệu mẫu | Phạm Tấn Tài |
| 2026-09-24 | Claude Code (Claude Opus 5.5) | `frontend/src/hooks/useAuth.js`, `frontend/src/layouts/ReviewerLayout.jsx`, `frontend/src/pages/reviewer/*`, `frontend/src/App.jsx`, `frontend/src/pages/auth/Login.jsx`, `frontend/src/locales/*` | Loại hỗ trợ: sửa code. Đổi vai trò Manager thành Reviewer (route `/reviewer/*`, link cũ `/manager/*` tự chuyển hướng); giữ phiên đăng nhập trong `sessionStorage` để audit log ghi đúng người thao tác | Test: build; E2E Chrome headless: đăng nhập Reviewer, tải lại trang vẫn giữ phiên, `/manager/team` chuyển về `/reviewer/dashboard` | Phạm Tấn Tài |
| 2026-09-24 | Claude Code (Claude Opus 5.5) | `frontend/src/services/{apiClient,textExtraction,documentProcessing}.js`, `frontend/src/utils/{chunker,injectionScan}.js`, `frontend/src/services/documentStore.js`, `frontend/src/contexts/DocumentsContext.jsx`, `frontend/src/components/DocumentProcessing.jsx` | Loại hỗ trợ: sinh code. Xử lý tài liệu (WBS Phase 2): trích xuất PDF (pdf.js) và DOCX (mammoth), chia chunk theo contract `{doc_id, chunk_id, section_id, heading, page, content}`, thanh tiến trình 0–100%, xem chunk; quét prompt injection EN/VI (Rules mục 5); nối sẵn `POST /upload` khi có `VITE_API_URL` | Test: 37 unit test (chunker + 11 kịch bản tấn công + văn bản bình thường không bị gắn cờ); E2E với 5 file thật (PDF 3 trang, DOCX có heading, MD, TXT tấn công, PDF chỉ có ảnh) trên cả dev và bản build | Phạm Tấn Tài |
| 2026-09-24 | Claude Code (Claude Opus 5.5) | `frontend/src/data/roleMatrix.js`, `frontend/src/utils/roleMatrix.js`, `frontend/src/contexts/RoleMatrixContext.jsx`, `frontend/src/pages/admin/RoleMatrix.jsx` | Loại hỗ trợ: sinh code + dữ liệu mẫu. Màn hình Role Requirement Matrix 10 vị trí × 20 tài liệu, chủ đề bắt buộc kèm từ khoá, nhập/xuất CSV, tính Coverage Score | Ma trận mặc định cần đối chiếu với `role_matrix.csv` của nhóm QA. Test: 8 unit test (coverage, CSV round-trip, dòng lỗi); E2E: bấm ô, tải lại trang vẫn giữ | Phạm Tấn Tài |
| 2026-09-24 | Claude Code (Claude Opus 5.5) | `frontend/src/utils/verification.js`, `frontend/src/services/pipelineService.js`, `frontend/src/contexts/ReviewContext.jsx`, `frontend/src/components/Citation.jsx`, `frontend/src/components/review/*`, `frontend/src/pages/review/*`, `frontend/src/pages/admin/AIStudio.jsx`, `frontend/src/pages/employee/Quiz.jsx`, `frontend/src/data/mock.js` | Loại hỗ trợ: sinh code. Phase 3: phân loại Verified / Verified with Warning / Manual Review Required; Dual Comparison View tô đỏ/vàng; hiển thị `exact_quote` và đối chiếu câu trích với chunk; Approve / Reject / Override kèm lý do; ghi đè từng trường; audit log chỉ thêm, xuất CSV. Bỏ `setTimeout` giả lập ở AI Studio | Output hai pipeline vẫn là dữ liệu mẫu, giao diện gắn nhãn *Chế độ demo*. Test: 17 unit test cho classifier và quy tắc quyết định; E2E đi hết luồng sinh → Override bị chặn khi lý do ngắn → ghi đè trường → phân loại lại → Approve → audit log 6 bản ghi; không lộ khoá dịch ở cả vi/en | Phạm Tấn Tài |
| 2026-09-24 | Claude Code (Claude Opus 5.5) | `frontend/src/pages/admin/Documents.jsx`, `frontend/src/components/UI.jsx`, `frontend/src/styles/review.css` | Loại hỗ trợ: sửa lỗi. Modal tải lên kẹt ở *Đang kiểm tra…* khi chạy dev (StrictMode làm cờ `mounted` không được bật lại); textarea ở AI Studio nằm cạnh nhãn; `Card` bỏ qua prop `style` | Test: tái hiện lỗi bằng E2E trên dev server, sửa xong chạy lại thì đạt | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `frontend/src/utils/{pathGenerator,pathChecks,pathWorkflow,progress}.js`, `frontend/src/contexts/{PathsContext,EnrollmentContext}.jsx`, `frontend/src/services/pipelineService.js` | Loại hỗ trợ: sinh code. Đổi phạm vi sản phẩm theo yêu cầu nhóm: web tạo lộ trình học từ tài liệu (không phải quản lý nhân sự). Sinh bản nháp lộ trình từ chunk; kiểm định kiến thức / luồng / chức năng / an toàn; vòng đời nháp → chờ duyệt → cần sửa → phát hành → thu hồi; tiến độ học và khoá giai đoạn | Test: 35 unit test mới (sinh lộ trình, kiểm định, phân quyền, tiến độ) — tổng 80/80 đạt. Bản nháp chưa phải output Gemini, giao diện gắn nhãn rõ | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `frontend/src/App.jsx`, `frontend/src/layouts/RoleLayout.jsx`, `frontend/src/hooks/useAuth.js`, `frontend/src/components/path/*`, `frontend/src/pages/{hr,reviewer,shared,employee}/*`, `frontend/src/styles/paths.css`, `frontend/src/locales/*` | Loại hỗ trợ: sinh code + dọn code. 3 vai trò HR / Reviewer / Nhân viên, chặn truy cập theo vai trò; Reviewer sửa trực tiếp, góp ý theo mục, trả về HR, phát hành theo phòng ban/vị trí; nhân viên học bài, mở tài liệu gốc đúng trang, làm nhiệm vụ, làm bài kiểm tra. Xoá các trang quản lý nhân sự và dữ liệu mẫu cố định (`data/mock.js`) | Test: build; E2E Chrome headless toàn luồng (tải 5 file → sinh → sửa → gửi duyệt → trả về → sửa → duyệt lại → phát hành → nhân viên học, trượt rồi đạt bài kiểm tra, giai đoạn sau mở); quét khoá dịch vi/en trên mọi trang → 0 lỗi; khoá en/vi khớp 594/594 | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `documentation/FRONTEND_FLOWS.md`, `frontend/README.md` | Loại hỗ trợ: soạn tài liệu. Mô tả chức năng, luồng thực hiện theo vai trò, vòng đời lộ trình, quy tắc kiểm định, cấu trúc dữ liệu, hợp đồng API và danh sách thay đổi | Đã đối chiếu với code và kết quả test thực tế | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `frontend/src/**`, `frontend/package.json`, `documentation/FRONTEND_FLOWS.md`, `frontend/README.md` | Loại hỗ trợ: dọn code. Quét file/export/CSS/khoá dịch không dùng; xoá trang đăng ký, quên mật khẩu và form đăng nhập giả; gỡ `recharts`; bỏ 24 icon, 223 selector CSS, 31 khoá dịch thừa; gộp CSS tính năng vào `features.css`; bỏ comment trang trí và comment mô tả "làm gì" | Test: build; 80/80 unit test; E2E toàn luồng chạy lại đạt; quét lại không còn file/export/import thừa | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `frontend/src/services/sanitize.js`, `frontend/src/components/ErrorBoundary.jsx`, `frontend/src/contexts/*`, `frontend/src/main.jsx`, `frontend/src/layouts/RoleLayout.jsx`, `frontend/src/App.jsx`, `frontend/index.html` | Loại hỗ trợ: sửa lỗi. Trắng trang khi trình duyệt còn dữ liệu cũ/hỏng: lọc dữ liệu khi đọc, thêm error boundary (trang báo lỗi + xoá dữ liệu demo), nút quay về ở trang 404, thêm favicon | Tái hiện lỗi bằng Chrome headless (5 loại dữ liệu hỏng × 3 vai trò), sửa xong không còn trang trắng; 6 unit test mới (86/86 đạt); E2E toàn luồng đạt | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `frontend/src/utils/pathChecks.js`, `frontend/src/utils/pathGenerator.js`, `frontend/src/services/pipelineService.js`, `frontend/src/pages/hr/CreatePath.jsx`, `frontend/src/components/path/*`, `frontend/src/App.jsx`, `frontend/src/layouts/RoleLayout.jsx`, `frontend/tests/*`, tài liệu | Loại hỗ trợ: sửa code theo yêu cầu nhóm. Bỏ Role Requirement Matrix khỏi frontend (thuộc Pipeline 2 backend Python): xoá trang, context, dữ liệu mặc định, hàm tính Coverage/CSV; kiểm định "Chức năng" chỉ hiển thị `coverage` backend trả về, chưa có thì cảnh báo | Test: build; 77/77 unit test; E2E toàn luồng đạt; quét lại không còn file/export/khoá dịch thừa | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `frontend/src/pages/auth/Login.jsx`, `frontend/src/layouts/AuthLayout.jsx`, `frontend/src/hooks/useAuth.js`, `frontend/src/contexts/LanguageContext.jsx`, `frontend/src/styles/*.css`, `frontend/src/locales/*` | Loại hỗ trợ: sinh code giao diện theo mẫu video *Modern Glassmorphism Login* và ảnh nhóm gửi. Giữ bố cục 2 nửa (giới thiệu + thẻ kính); form email/mật khẩu kiểm tra với 3 tài khoản demo, hiện/ẩn mật khẩu, ghi nhớ đăng nhập (localStorage/sessionStorage), hướng dẫn quên mật khẩu, chip điền sẵn tài khoản | Test Chrome headless: bỏ trống / sai mật khẩu / hiện mật khẩu / ghi nhớ / đăng xuất; ảnh chụp 1440px, 390px, tiếng Anh; 77/77 unit test; E2E toàn luồng đạt | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `frontend/src/hooks/useMyPaths.js`, `frontend/src/utils/helpers.js`, `frontend/README.md`, `documentation/FRONTEND_FLOWS.md` | Loại hỗ trợ: rà soát trước khi push. Quét file/export/import/CSS/khoá dịch/thư viện không dùng, `console.log`/TODO/chuỗi giống API key; chuyển `useMyPaths` về `hooks/`; thống nhất comment tiếng Việt | Không còn code thừa (các class CSS còn lại là class ghép động); build; 77/77 unit test; E2E toàn luồng đạt | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `backend/**` (app, alembic, tests, README, requirements), `.gitignore`, `frontend/.env.example`, `frontend/src/services/apiClient.js` | Loại hỗ trợ: sinh code + thiết kế. Dựng khung backend FastAPI theo Rules mục 2: config `.env`, SQLAlchemy 2 + Alembic (SQLite dev / PostgreSQL deploy), 13 bảng cho tài liệu, chunk, cờ injection, lộ trình (nội dung JSON), nguồn, phát hành, góp ý, audit log, tiến độ học; seed 10 phòng ban, 10 vị trí, 3 tài khoản demo (bcrypt); đăng nhập JWT, `/auth/me`, phân quyền theo vai trò, danh mục; API dưới `/api`; hook Alembic bỏ CHECK trùng của enum | Test: 26/26 pytest (đăng nhập 3 vai trò, sai mật khẩu, token sai/hết hạn, tài khoản bị khoá, phân quyền, CORS, ràng buộc DB, seed chạy lại, migration khớp model); `alembic check` không lệch; sinh lại migration từ DB trống khớp bản commit; cài `requirements.txt` trên venv sạch, `pip check` đạt; chạy uvicorn thật và gọi login/danh mục/Swagger | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `backend/app/{ingestion,services,schemas,api/routes}/*`, `backend/app/core/{errors,injection_filter}.py`, `backend/tests/*`, `backend/README.md` | Loại hỗ trợ: sinh code. Backend phần HR: kho tài liệu (tải lên kèm kiểm tra định dạng/magic bytes/dung lượng/trùng SHA-256/mã/phiên bản; trích xuất PDF, DOCX, TXT, MD, CSV; chunker và bộ lọc prompt injection chuyển từ frontend; vòng đời phiên bản; xem chunk; tải file gốc; thử lại; xoá có chặn khi đang dùng); lộ trình (tạo, sửa, sinh lại, gửi duyệt, gửi lại, xoá nháp, thu hồi, góp ý, đánh dấu đã xử lý) với bảng quyền kiểm tra ở server; audit log ghi cùng transaction; lọc dữ liệu theo vai trò; lỗi trả `code` trùng khoá dịch frontend | Test: 85/85 pytest (thêm 59 test: trích xuất file thật, PDF scan, mã hoá sai, luật injection EN/VI, API tài liệu, vòng đời lộ trình, phân quyền 403/404/409, audit); so khớp chunker Python với chunker JS trên 8 bộ dữ liệu → giống hệt; chạy server thật với 5 tài liệu mẫu của nhóm (PDF/DOCX/MD) → xử lý 16–150 ms, tạo lộ trình và gửi duyệt ghi đúng audit | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `backend/app/genai_pipeline/*` (client, schemas, prompts v1.0, grounding, local_draft, generator), `backend/app/services/{paths,path_checks}.py`, `backend/app/db/base.py`, migration `path generation report`, `frontend/src/{services/apiClient,services/apiMappers,hooks/useAuth,contexts/DocumentsContext,contexts/PathsContext}.js(x)`, `frontend/src/components/path/{GenerationReport,PathContent,PathComments}.jsx`, `frontend/src/pages/*`, `frontend/src/locales/*` | Loại hỗ trợ: sinh code + thiết kế. Pipeline 1: Gemini (google-genai, structured output) sinh bài học + nhiệm vụ rồi câu hỏi cho từng học phần song song; grounding loại/sửa mục không trích được nguyên văn tài liệu, câu hỏi có đáp án ngoài câu trích; bản nháp theo luật (chuyển từ pathGenerator.js) khi không có key hoặc Gemini lỗi; báo cáo sinh nội dung. Kiểm định phía server (chuyển từ pathChecks.js) khi gửi duyệt/duyệt; API Reviewer approve / request-changes. Frontend chạy được với backend: đăng nhập JWT, tài liệu, lộ trình, tab "Sinh bằng AI". Sửa lỗi giờ lệch 7 tiếng do SQLite bỏ múi giờ | Test: 110/110 pytest (Gemini giả đọc prompt thật, gài lỗi trích dẫn / đáp án / sập API); bộ sinh bản nháp Python so với JS trên 4 tài liệu mẫu × 6 cấu hình → giống hệt; frontend build + 77/77 Vitest; E2E Chrome headless với backend: HR tải 3 tài liệu → sinh lộ trình → gửi duyệt → Reviewer phát hành → nhân viên mở học phần, không lỗi console. Chưa chạy với Gemini thật (chưa có key) | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `frontend/src/utils/chunker.js`, `backend/app/ingestion/chunker.py`, `frontend/tests/chunker.test.js`, `backend/tests/test_ingestion.py` | Loại hỗ trợ: rà soát dữ liệu + sửa lỗi. Đánh giá `sample_documents/` và `role_matrix/` nhánh `feat/le-thi-kieu-duyen` bằng pipeline backend. Phát hiện chunker nhận nhầm dòng PDF bị ngắt ("5 days until 31 March…") là tiêu đề đánh số, làm cắt đôi mục §6 của DOC-02 (ca kiểm thử mâu thuẫn). Sửa: tiêu đề đánh số phải bắt đầu bằng chữ in hoa, ở cả frontend và backend | Test: 112/112 pytest, 78/78 Vitest; so khớp chunker và bộ sinh bản nháp Python với JS → vẫn giống hệt; quét lại 16 file mẫu → không còn tiêu đề nhận nhầm | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `sample_documents/DOC-11…DOC-20_*.pdf`, `sample_documents/source/*.md`, `sample_documents/source/build_pdfs.mjs`, `sample_documents/README_PHASE2.md` | Loại hỗ trợ: soạn tài liệu mẫu + công cụ. Viết 10 tài liệu còn thiếu của danh mục (SOP onboarding, sổ tay vận hành chi nhánh, 3 bộ mô tả công việc, FAQ, 4 tài liệu kiểm thử: mâu thuẫn, prompt injection, quy định hết hạn, ngoại lệ phòng ban), số liệu khớp DOC-01…10; lấp khoảng trống R040/R048 của ma trận vai trò; build PDF bằng Chrome headless | Mỗi PDF 3–5 trang; chạy qua pipeline backend: trích xuất + chia chunk không nhận nhầm tiêu đề, mỗi tài liệu sinh 5 câu hỏi và 2–3 nhiệm vụ; DOC-18 bị gắn cờ đủ 9/9 luật ở 4 chunk, 3 biến thể né lọc không bị bắt (đúng thiết kế); đáp án kiểm thử ghi trong README_PHASE2. Nội dung là dữ liệu giả lập cho công ty hư cấu, cần nhóm QA đọc duyệt | Phạm Tấn Tài |
| 2026-09-25 | Claude Code (Claude Opus 5.5) | `frontend/src/utils/chunker.js`, `backend/app/ingestion/chunker.py`, tests, `sample_documents/README_PHASE2.md` | Loại hỗ trợ: kiểm thử dữ liệu + sửa lỗi. Chạy toàn bộ DOC-01…20 qua API vào DB mới: tải lên, vòng đời phiên bản, cờ injection, sinh lộ trình cho 10 vị trí, kiểm định phía server, đối chiếu ma trận vai trò, luồng duyệt. Phát hiện lỗi chunker thứ hai ("31 December. This rule…" bị coi là tiêu đề, cắt DOC-02 §6); siết luật tiêu đề đánh số: không có dấu kết thúc câu bên trong, tối đa 80 ký tự sau số (tiêu đề thật dài nhất trong 290 tiêu đề của 20 tài liệu là 68 ký tự) | Kết quả: 20/20 tài liệu xử lý xong, 0 tiêu đề nhận nhầm trên 285 chunk; DOC-02 obsolete, DOC-19 expired; DOC-18 loại đúng 4 chunk, 0 injection lọt vào lộ trình; 10/10 lộ trình không lỗi chặn, 100% trích dẫn khớp; 114/114 pytest, 79/79 Vitest; chunker và bộ sinh bản nháp Python vẫn giống hệt JS | Phạm Tấn Tài |

---

## II. QUY TẮC ĐẠO ĐỨC & KIỂM SOÁT AI CỦA NHÓM

1. **Không sao chép nguyên văn (Zero Raw Copy-Paste):** Mọi đoạn code được gợi ý đều được refactor lại theo phong cách sinh viên tự nhiên, đặt tên biến phù hợp với ngữ cảnh thực tế của dự án.
2. **Không phụ thuộc AI ở luồng Ground-Truth (Pure Python Rule Engine):** Module kiểm chứng đối soát trong `src/comparison_engine/` và `src/contradiction_checks/` tuyệt đối không dùng bất kỳ SDK AI nào để đảm bảo tính khách quan và độc lập 100%.
3. **Bảo mật thông tin tối đa:** Không bao giờ đưa tài liệu nhạy cảm hay API Key của thành viên lên các dịch vụ đám mây công cộng mà không qua mã hóa hoặc kiểm soát.
