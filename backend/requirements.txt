# Tested on Python 3.14 (also fine on 3.12+). Install:  pip install -r requirements.txt

# API
fastapi==0.141.1
uvicorn[standard]==0.53.0
python-multipart==0.0.32
email-validator==2.3.0

# Settings & validation
pydantic==2.13.5
pydantic-settings==2.15.0

# Database
SQLAlchemy==2.1.0
alembic==1.20.0
psycopg[binary]==3.3.6

# Auth (PyJWT + bcrypt replace python-jose and passlib, which are unmaintained)
PyJWT==2.15.0
bcrypt==5.0.0

# Ingestion
pymupdf==1.28.2
python-docx==1.2.0

# Pipeline 1 (google-genai replaces the deprecated google-generativeai)
google-genai==2.25.0

# Tests & lint  (ruff check app tests alembic)
pytest==9.1.1
httpx2==2.13.1
ruff==0.16.9
